import mockWidget from "./mock-widget.png";

export default function OfferDetailWidget() {
  {
    /* TODO: Remove widget mock after implementation */
  }
  return (
    <img
      src={mockWidget}
      style={{ marginTop: "-5rem", marginLeft: "-6.5rem" }}
    />
  );
}
